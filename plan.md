# Skuld — MVP Plan

> **Brand Name:** Skuld
> **Tagline:** *"Every 'should' gets a test."*
> **PyPI Name:** `skuld`
> **Import Name:** `skuld`
> **CLI Command:** `skuld`
> **GitHub Repo:** `Skuld`
> **One-liner:** AI-powered test case generator with adversarial cross-model review, RTM-based deterministic scoring, and confidence assessment.
> **Mythology:** In Norse myth, Skuld is the Norn who weaves what *should become*. Acceptance criteria define what must be true — Skuld ensures every "should" has a test proving it.
> **Last Updated:** 2026-07-16

---

## Position in Pipeline

```
PRD/Story → [QEStrategyForge] → Strategy
Strategy + ACs → [Skuld] → Test Cases + RTM + Confidence Score
Test results → [SuiteCompass] → Optimised Suite
Release context → [ReleaseRadar] → Risk Score
                        ↑
               [Orchestrator (future)] wires all four flagships
```

---

## Architecture — Two-Pass Adversarial Review

```
Structured ACs (YAML)
       ↓
┌─────────────────────┐
│  Generator LLM      │  Pass 1: produce test cases
│  (e.g. Claude)      │  (functional, negative, edge-case)
└────────┬────────────┘
         ↓
┌─────────────────────┐
│  Reviewer LLM       │  Adversarial review: score coverage gaps,
│  (e.g. GPT)         │  flag weak tests, identify missing scenarios
└────────┬────────────┘
         ↓
┌─────────────────────┐
│  Generator LLM      │  Pass 2: refine test suite using review feedback
│  (e.g. Claude)      │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│  Deterministic       │  RTM build + scoring
│  Scorer              │
└────────┬────────────┘
         ↓
Final output: test cases + RTM + confidence score + coverage report
```

V2 extension: configurable iteration depth, threshold-based termination loop.

---

## Scoring Model — Hybrid Deterministic + Qualitative

```
Final Score = (0.70 × RTM_score) + (0.30 × adversarial_score)
```

### Deterministic Component (RTM + AC Matching) — 70%

| Check | Measurement | Type |
|-------|-------------|------|
| AC coverage | `covered_ACs / total_ACs` | Binary per AC |
| Negative coverage | Every AC has ≥1 negative test | Binary per AC |
| Edge-case coverage | Every AC has ≥1 edge-case test | Binary per AC |
| Orphan tests | Tests not mapped to any AC | Count |
| Type distribution | functional / negative / edge ratios meet minimums | Threshold |

### Qualitative Component (Adversarial Review) — 30%

| Check | Measured by |
|-------|-------------|
| Assertion strength | Reviewer LLM |
| Scenario realism | Reviewer LLM |
| Edge-case quality | Reviewer LLM |

---

## Coverage Types

| Type | MVP Status |
|------|------------|
| Functional (happy path) | **Non-negotiable** |
| Negative (error paths, invalid inputs) | **Non-negotiable** |
| Edge-case (boundaries, nulls, concurrency) | **Non-negotiable** |
| NFR (performance, security) | Deferred to V2 |
| Accessibility | Deferred to V2 |

---

## Input Contract

### Prerequisite: Approved Strategy (HITL Gate)

Before Skuld generates test cases, the following MUST be true:
1. A test strategy for the story/epic has been generated by QEStrategyForge (or equivalent).
2. The strategy has been **reviewed and approved by the user** (human-in-the-loop).
3. The approved strategy is provided as input (via `strategy_ref`) or its key decisions are embedded in the input YAML.

**Rationale:** Test cases must align with a validated strategy. Generating tests from raw ACs alone risks misalignment with the team's testing approach. User approval ensures the strategy reflects real-world constraints, risk appetite, and team capacity before test cases are derived from it.

**Enforcement:** If `strategy_ref` is provided, the input loader checks for an `approved: true` flag or a `reviewed_by` field. If absent, Skuld emits a warning and requests confirmation before proceeding.

### Primary Input: `story_input.yaml`

```yaml
story:
  id: "PROJ-1234"
  title: "User login with MFA"
  description: |
    As a registered user, I want to log in using multi-factor authentication
    so that my account is protected from unauthorised access.

acceptance_criteria:
  - id: "AC-1"
    description: "User can log in with valid email and password"
    criticality: high          # high | medium | low — drives Priority
  - id: "AC-2"
    description: "MFA code is sent to registered phone number after password validation"
    criticality: high
  - id: "AC-3"
    description: "Login fails after 3 incorrect MFA attempts with account lock"
    criticality: medium
  - id: "AC-4"
    description: "User sees meaningful error message for expired MFA code"
    criticality: low

# Optional: user-supplied constraints
config:
  generator_model: "claude"       # default: claude
  reviewer_model: "gpt"          # default: gpt (must differ from generator)
  min_negative_per_ac: 1         # default: 1
  min_edge_case_per_ac: 1        # default: 1
  output_format: "markdown"      # markdown | yaml | json (default: markdown)
```

### Optional Input: QEStrategyForge Strategy Output

```yaml
# If provided, type distribution minimums are derived from strategy
strategy_ref:
  path: "path/to/strategy_output.md"
  approved: true                     # REQUIRED — HITL gate
  reviewed_by: "sandeep.singh"       # Who approved the strategy
  # OR inline excerpt:
  testing_types_required:
    - functional
    - negative
    - edge_case
    - security          # would be deferred to V2 but noted in coverage report
```

### Optional Input: Custom Test Case Template

```yaml
# If provided, overrides the default column set
template:
  columns:
    - "Test Case ID"
    - "Story/Req ID"
    - "Acceptance Criteria"
    # ... user defines their own
```

If not provided, the default template (below) is used.

---

## Output Contract

### 1. Test Case Table (Markdown / YAML / JSON)

**Default columns:**

| # | Column | Filled by | Description |
|---|--------|-----------|-------------|
| 1 | Test Case ID | Agent | Auto-generated (TC-001, TC-002, ...) |
| 2 | Story/Req ID | Agent | From input story.id |
| 3 | Acceptance Criteria | Agent | AC-id(s) this test covers |
| 4 | Test Type | Agent | functional / negative / edge-case |
| 5 | Priority | Agent | P1 / P2 / P3 (derived from AC criticality) |
| 6 | Preconditions | Agent | Setup state required |
| 7 | Test Steps | Agent | Numbered action steps |
| 8 | Expected Result | Agent | Observable outcome |
| 9 | Test Data | Agent | Specific values / datasets |
| 10 | Automatable | Agent | Y / N (inferred from test nature) |
| 11 | Review Flag | Adversarial reviewer | ✓ (OK) or ⚠ (flagged concern) |
| 12 | Automated | Team (later) | Default "N" — lifecycle tracking |
| 13 | Script Group | Team (later) | Clusters tests automated under one script |

### 2. Requirements Traceability Matrix (RTM) — Persistent Artifact

The RTM is a **persistent, versioned artifact** stored as `rtm.yaml` in the target project. It accumulates across multiple Skuld runs (one per story) and serves as the source of truth for test coverage enforcement.

**Schema (`rtm.yaml`):**

```yaml
rtm_version: "1.0"
last_updated: "2026-07-16T14:30:00Z"
project_id: "PROJ"

entries:
  - test_case_id: "TC-001"
    story_id: "PROJ-1234"
    ac_id: "AC-1"
    test_type: "functional"       # functional | negative | edge-case
    priority: "P1"
    added_date: "2026-07-16"
    added_by_run: "run-20260716-143000"   # links to generation run
    status: "active"              # active | deprecated | superseded

  - test_case_id: "TC-002"
    story_id: "PROJ-1234"
    ac_id: "AC-1"
    test_type: "negative"
    priority: "P2"
    added_date: "2026-07-16"
    added_by_run: "run-20260716-143000"
    status: "active"

coverage_summary:
  total_stories: 3
  total_acs: 12
  total_test_cases: 45
  ac_coverage: 11/12             # 91.7%
  negative_coverage: 9/12        # 75.0%
  edge_case_coverage: 7/12       # 58.3%
  orphan_tests: 2
  gaps:
    - ac_id: "AC-7"
      story_id: "PROJ-1236"
      missing: ["negative", "edge-case"]
      gap_since: "2026-06-20"    # how long this gap has existed
```

**Display format (rendered in reports):**

```
          TC-001  TC-002  TC-003  TC-004  TC-005  TC-006
AC-1        F       N       E       -       -       -
AC-2        -       -       -       F       N       -
AC-3        -       -       -       -       -       F
AC-4        -       N       -       -       -       -

Legend: F=functional, N=negative, E=edge-case, -=not covered
```

**CLI commands for RTM management:**

```bash
skuld generate story.yaml --rtm-file rtm.yaml   # generate + merge into persistent RTM
skuld rtm update story.yaml --rtm-file rtm.yaml # merge without regeneration (manual test cases)
skuld rtm report --rtm-file rtm.yaml            # coverage enforcement report + gap age
skuld rtm gaps --rtm-file rtm.yaml              # list uncovered/partially-covered ACs
skuld rtm history --rtm-file rtm.yaml --ac AC-3 # show coverage timeline for one AC
```

### 3. Confidence Score Report

```yaml
confidence_score:
  overall: 82.3
  breakdown:
    rtm_score: 87.5          # deterministic — 70% weight
    adversarial_score: 70.0  # qualitative — 30% weight
  rtm_details:
    ac_coverage: 4/4         # 100%
    negative_coverage: 3/4   # AC-3 missing negative test
    edge_case_coverage: 2/4  # AC-2, AC-4 missing edge tests
    orphan_tests: 0
    type_distribution:
      functional: 40%        # minimum 30% — PASS
      negative: 35%          # minimum 25% — PASS
      edge_case: 25%         # minimum 20% — PASS
  gaps:
    - "AC-3: no negative test — what happens with 2 incorrect attempts (below threshold)?"
    - "AC-2: no edge-case test — MFA delivery to invalid phone format?"
    - "AC-4: no edge-case test — expired code reused after refresh?"
```

### 4. Assertions File (for Benchmarking)

```yaml
must_include_headings:
  - "## Test Cases"
  - "## Requirements Traceability Matrix"
  - "## Confidence Score"
  - "## Coverage Gaps"

must_include_labels:
  - "Overall Confidence:"
  - "AC Coverage:"
  - "Orphan Tests:"

must_include_substrings:
  - "functional"
  - "negative"
  - "edge-case"
```

---

## Module Map (Planned)

```
src/skuld/
├── __init__.py
├── cli.py                  # Click CLI: skuld generate, skuld rtm, skuld benchmark, skuld score
├── models.py               # Story, AcceptanceCriterion, TestCase, RTMEntry, RTMStore, ConfidenceScore
├── input_loader.py         # Parse + validate story_input.yaml
├── output_validator.py     # Validate output against assertions
├── ac_extractor.py         # Extract/normalise ACs from input
├── test_generator.py       # LLM-based test case generation (Pass 1 + Pass 2)
├── adversarial_reviewer.py # LLM-based adversarial review (cross-model)
├── rtm_builder.py          # Build RTM from test cases + ACs (per-run)
├── rtm_store.py            # Persistent RTM: load, merge, save, query, gap history
├── rtm_scorer.py           # Deterministic scoring from RTM
├── confidence_scorer.py    # Combine RTM score + adversarial score
├── renderer.py             # Render output (markdown / yaml / json)
├── end_to_end_flow.py      # Orchestrate full pipeline
├── benchmark_runner.py     # Run benchmark scenarios
├── llm_client.py           # LLM provider abstraction
└── prompt_builder.py       # Prompt templates for generator + reviewer
```

---

## Phased Build Plan

### Phase 1 — Scaffold + Core Data Model
- [x] T1: Project scaffold (pyproject.toml, src/, tests/, benchmarks/, AGENTS.md, plan.md, claude-memory/)
- [x] T2: Models (Story, AcceptanceCriterion, TestCase, RTMEntry, ConfidenceScore) — 26 tests passing
- [x] T3: Input loader (parse story_input.yaml, validate ACs, reject invalid) — 31 tests passing
- [x] T4: Output validator (validate generated output against assertions YAML) — 15 tests passing

### Phase 2 — Deterministic Engine (No LLM)
- [x] T5: RTM builder (given test cases + ACs, build traceability entries per run) — 23 tests passing
- [x] T5b: RTM store (persistent RTM: load, merge, save, query, gap history) — 37 tests passing
- [ ] T6: RTM scorer (deterministic scoring: AC coverage, type distribution, orphans)
- [ ] T7: Confidence scorer (combine RTM score + placeholder adversarial score)
- [ ] T8: Renderer (output markdown / yaml / json from test cases + RTM + score)

### Phase 3 — LLM Generation + Adversarial Review
- [ ] T9: LLM client abstraction (pluggable: Claude, GPT, Ollama)
- [ ] T10: Prompt builder (generator prompt + reviewer prompt templates)
- [ ] T11: Test generator (Pass 1 — generate from ACs; Pass 2 — refine from review)
- [ ] T12: Adversarial reviewer (cross-model review, produce review feedback + score)

### Phase 4 — Integration
- [ ] T13: End-to-end flow (wire: input → generate → review → refine → RTM build → RTM merge → score → render)
- [ ] T14: CLI (skuld generate, skuld rtm update/report/gaps/history, skuld benchmark, skuld score)
- [ ] T15: Benchmarks (≥3 scenarios with input + assertions files)
- [ ] T16: Coverage hardening (all modules ≥90%, all benchmarks green)

---

## Dependency Graph

```
T1 (scaffold) ─┬─► T2 (models) ─► T3 (input loader) ─► T5 (RTM builder)
               │                                              │
               │                                        T5b (RTM store)
               │                                              │
               │                  T4 (output validator) ──► T6 (RTM scorer) ─► T7 (confidence)
               │                                                                    ↓
               │                                                              T8 (renderer)
               │
               └─► T9 (LLM client) ─► T10 (prompt builder) ─► T11 (generator)
                                                                    ↓
                                                              T12 (reviewer)
                                                                    ↓
                                              T13 (E2E flow: deterministic + LLM + RTM persist)
                                                                    ↓
                                                              T14 (CLI) ─► T15 (benchmarks)
                                                                                ↓
                                                                          T16 (hardening)
```

Phase 2 (T5–T5b–T8) and Phase 3 (T9–T12) can run in parallel.
Phase 4 (T13–T16) requires both Phase 2 and Phase 3.

**Build order constraint:** Phase 2 (deterministic engine) must be GREEN before Phase 3 (LLM layer) integrates with it. LLM code must never leak into deterministic modules.

---

## Guardrails Summary

| Category | Rule | Enforcement |
|----------|------|-------------|
| Architecture | `rtm_builder`, `rtm_scorer`, `confidence_scorer` are LLM-free | Import check in tests |
| Architecture | Generator and Reviewer use structured `ReviewFeedback` schema | Schema validation in `adversarial_reviewer.py` |
| Architecture | One LLM call = one responsibility | Prompt templates enforce separation |
| Security | User content fenced in prompts with delimiters | Prompt builder wraps in `<user_content>` tags |
| Security | API keys in env vars only | No key parameters in config YAML; `.env` in `.gitignore` |
| Cost | Token budget cap: 32K total per run (configurable) | `llm_client.py` tracks and enforces |
| Cost | AC count limit: warn >15, reject >30 | `input_loader.py` validation |
| Quality | Reject truncated/unparseable LLM responses | `test_generator.py` + `adversarial_reviewer.py` validate `finish_reason` |
| Quality | Benchmark gate: all benchmarks green before merge | CI + `skuld benchmark` command |
| Scope | MVP: tabular specs only, no code gen, no NFR, single refinement pass | Plan + AGENTS.md working rules |

---

## Test Count Targets

| File | Min |
|---|---|
| test_models.py | 10 |
| test_input_loader.py | 15 |
| test_output_validator.py | 12 |
| test_rtm_builder.py | 15 |
| test_rtm_store.py | 18 |
| test_rtm_scorer.py | 20 |
| test_confidence_scorer.py | 8 |
| test_renderer.py | 12 |
| test_generator.py | 10 |
| test_adversarial_reviewer.py | 10 |
| test_end_to_end_flow.py | 5 |
| test_cli.py | 12 |
| test_pipeline_integration.py | 10 |
| test_contracts.py | 3 |
| **Total** | **≥160** |

---

## Naming Rationale

| Attribute | Value | Rationale |
|-----------|-------|-----------|
| Brand | **Skuld** | Norse Norn of "what should become" — ACs define what must be true |
| PyPI | `skuld` | Short, memorable, unique |
| CLI | `skuld` | One word, no ambiguity, consistent with short mnemonics (`iro`, `relrisk`) |
| GitHub | `Skuld` | Brand name for repository |

---

## Detailed Action Plan

### Phase 1 — Scaffold + Core Data Model

> Reuse details: see [docs/REUSE-PLAN.md](docs/REUSE-PLAN.md) — Phase 1 section

#### T2: Models
- **Reused (from IRO):** `FlowResult`, `ValidationResult`, `InputPackage`, exit codes, `GenerationRequest/Response`
- **New (TDD):**
  - `Story(id, title, description)`
  - `AcceptanceCriterion(id, description, criticality)` — criticality: high/medium/low
  - `TestCase(id, story_id, ac_ids, test_type, priority, preconditions, steps, expected_result, test_data, automatable, review_flag)`
  - `RTMEntry(ac_id, test_case_id, test_type)`
  - `ConfidenceScore(overall, rtm_score, adversarial_score, rtm_details, gaps)`
  - `ReviewFeedback(flagged_tests, missing_scenarios, quality_scores, suggestions)`
- **Tests:** dataclass instantiation, defaults, `is_valid` property, serialization

#### T3: Input Loader
- **Reused (from IRO):** `InputValidationError`, `_require_keys()`, two-level loader pattern
- **New (TDD):**
  - Story validation: `id`, `title`, `description` required
  - AC validation: `id`, `description` required; `criticality` defaults to `medium`
  - AC count guardrail: warn >15, reject >30
  - HITL gate: `strategy_ref.approved` or `strategy_ref.reviewed_by` must be present if strategy_ref is provided
  - Config defaults: fill missing model, threshold, format fields
- **Tests:** valid load, missing keys, invalid criticality, AC count limits (15/16/30/31), strategy approval enforcement, config defaults

#### T4: Output Validator
- **Reused (from IRO):** `parse_sections()` markdown parser, heading/label/placement checking pattern
- **New (TDD):**
  - `REQUIRED_HEADINGS`: `## Test Cases`, `## Requirements Traceability Matrix`, `## Confidence Score`, `## Coverage Gaps`
  - `REQUIRED_LABELS`: `Overall Confidence:`, `AC Coverage:`, `Negative Coverage:`, `Edge-Case Coverage:`, `Orphan Tests:`
  - `LABEL_SECTION_MAP`
  - `validate_output(markdown) -> ValidationResult`
- **Tests:** headings present/missing, labels in correct/wrong section, complete valid output passes

#### Support: conftest.py
- **Reused (from IRO):** `fixtures_dir`, `benchmarks_dir`, `repo_tmp` fixtures

---

### Phase 2 — Deterministic Engine (No LLM)

> Reuse details: see [docs/REUSE-PLAN.md](docs/REUSE-PLAN.md) — Phase 2 section

#### T5: RTM Builder
- **No reuse — Skuld's unique logic**
- **Implementation (TDD):**
  - `build_rtm(test_cases, acceptance_criteria) -> list[RTMEntry]`
  - `rtm_to_matrix(rtm_entries, ac_list, test_case_list) -> dict`
  - `find_gaps(rtm_entries, ac_list) -> list[str]`
- **Tests:** full coverage, partial coverage, orphan tests, empty inputs, multi-mapping

#### T5b: RTM Store (Persistent RTM)
- **No reuse — Skuld's unique persistence layer**
- **Implementation (TDD):**
  - `RTMStore` class:
    - `load(path: str) -> RTMStore` — load existing `rtm.yaml` (or create empty)
    - `merge(new_entries: list[RTMEntry], story_id: str, run_id: str) -> MergeResult`
      - Add new entries, mark superseded entries, update `coverage_summary`
      - Detect conflicts (same AC + test_type from different stories)
    - `save(path: str)` — write back to `rtm.yaml`
    - `query_gaps() -> list[GapInfo]` — uncovered/partially-covered ACs with gap age
    - `query_history(ac_id: str) -> list[HistoryEntry]` — coverage timeline for one AC
    - `deprecate(test_case_ids: list[str])` — mark tests as deprecated (not deleted)
    - `coverage_summary() -> dict` — aggregate stats
  - `MergeResult(added: int, superseded: int, conflicts: list[str])`
  - `GapInfo(ac_id, story_id, missing_types: list[str], gap_since: date)`
  - `HistoryEntry(date, run_id, action, test_case_id)`
- **Tests:**
  - Load empty RTM (new project) → valid empty store
  - Load existing RTM → entries parsed correctly
  - Merge new story → entries added, summary updated
  - Merge overlapping story → superseded entries marked, no duplicates
  - Query gaps → returns ACs missing coverage types
  - Query history → chronological entries for AC
  - Deprecate → status changes, not deleted
  - Save + reload round-trip → identical data
  - Gap age calculation (today - gap_since)

#### T6: RTM Scorer
- **Reused (from ReleaseRadar):** multi-factor `_compute_*` pattern, constants structure, `_assign_tier()`
- **New (TDD):**
  - `_compute_ac_coverage()`, `_compute_negative_coverage()`, `_compute_edge_coverage()`
  - `_compute_orphan_penalty()`, `_compute_type_distribution()`
  - `score_rtm(rtm_entries, ac_list, config) -> float`
- **Tests:** perfect score, zero score, partial scenarios, orphan penalty, distribution below minimum, boundary values

#### T7: Confidence Scorer
- **No reuse — simple combiner**
- **Implementation (TDD):**
  - `compute_confidence(rtm_score, adversarial_score, weights=(0.70, 0.30)) -> ConfidenceScore`
  - Fallback: adversarial_score=None → RTM-only
  - Clamp to 0–100
- **Tests:** normal 70/30, None fallback, edge values

#### T8: Renderer
- **Reused (from IRO):** line-accumulator pattern, section-by-section structure
- **New (TDD):**
  - Render test case table (13 columns), RTM matrix, confidence breakdown, coverage gaps
  - Support formats: markdown (primary), YAML, JSON
- **Tests:** headings/labels present, table dimensions, round-trip passes `validate_output()`, YAML/JSON parseable

---

### Phase 3 — LLM Generation + Adversarial Review

> Reuse details: see [docs/REUSE-PLAN.md](docs/REUSE-PLAN.md) — Phase 3 section

#### T9: LLM Client
- **Reused (from QEStrategyForge):** `LLMClient(Protocol)`, `FakeLLMClient`, `GenerationRequest/Response`
- **New (TDD):**
  - Token tracking + budget enforcement (`max_tokens_per_run`)
  - `finish_reason` validation (reject truncated)
  - Provider implementations: `ClaudeLLMClient`, `OpenAILLMClient`
- **Tests:** FakeLLMClient works, budget exceeded raises error, truncated rejected, Protocol compliance

#### T10: Prompt Builder
- **Reused (from QEStrategyForge):** template-composition, contract-injection, template-loading patterns
- **New (TDD):**
  - Generator prompt: role + story/ACs + constraints + contract injection + `<story_context>` fencing
  - Reviewer prompt: adversarial role + test cases + rubric + `ReviewFeedback` JSON schema
  - Refinement prompt: original tests + feedback + gap-fill instruction
- **Tests:** prompt contains all ACs, fence delimiters present, contract injected, rubric included

#### T11: Test Generator
- **No reuse — Skuld's unique orchestration**
- **Implementation (TDD):**
  - `generate_tests(input_package, llm_client, prompt_builder) -> list[TestCase]` (Pass 1)
  - `refine_tests(test_cases, review_feedback, input_package, llm_client, prompt_builder) -> list[TestCase]` (Pass 2)
  - Response parsing: structured extraction, field validation, AC mapping
- **Tests:** valid parse from FakeLLM, malformed → `GenerationError`, refinement incorporates feedback, budget enforced

#### T12: Adversarial Reviewer
- **No reuse — Skuld's unique module**
- **Implementation (TDD):**
  - `review_tests(test_cases, acceptance_criteria, llm_client, prompt_builder) -> ReviewFeedback`
  - `compute_adversarial_score(review_feedback) -> float`
  - Review flags (✓/⚠) mapped to test case IDs
- **Tests:** valid ReviewFeedback parsed, malformed → `ReviewParseError`, score computation, flag mapping

---

### Phase 4 — Integration

> Reuse details: see [docs/REUSE-PLAN.md](docs/REUSE-PLAN.md) — Phase 4 section

#### T13: End-to-End Flow
- **Reused (from IRO):** dual-entry pattern, `_run_from_package()`, self-validation
- **New (TDD):**
  - Pipeline: `load_input → generate → review → refine → build_rtm → score → confidence → render`
  - Error short-circuit via `FlowResult`
  - HITL gate: warn if strategy approval missing
- **Tests:** happy path, input error, LLM failure, self-validation failure, strategy warning

#### T14: CLI
- **Reused (from IRO):** Click group, `_emit()`, exit code mapping
- **New (TDD):**
  - `skuld generate <input.yaml> [--output FILE] [--format markdown|yaml|json] [--rtm-file rtm.yaml]`
    - Generates test cases AND merges into persistent RTM if `--rtm-file` provided
  - `skuld rtm update <input.yaml> --rtm-file rtm.yaml`
    - Merge manually-authored test cases into existing RTM (no LLM)
  - `skuld rtm report --rtm-file rtm.yaml`
    - Coverage enforcement report: % covered, gaps by AC, gap age
  - `skuld rtm gaps --rtm-file rtm.yaml`
    - List uncovered/partially-covered ACs
  - `skuld rtm history --rtm-file rtm.yaml --ac AC-3`
    - Coverage timeline for one AC
  - `skuld benchmark <input.yaml> <assertions.yaml>`
  - `skuld score <test_cases.yaml> <acs.yaml>` — deterministic-only mode
- **Tests:** generate with/without --rtm-file, rtm subcommands (update/report/gaps/history), benchmark pass/fail, score, --help

#### T15: Benchmarks
- **Reused (from IRO):** `benchmark_runner.py` verbatim (import rename only)
- **New scenarios:**
  - `login-mfa` — 4 ACs, basic generation + RTM + scoring
  - `ecommerce-checkout` — 8+ ACs, scale + type distribution
  - `incomplete-story` — 1 AC, graceful handling + gap reporting
- **Tests:** all scenarios pass assertions, runner reports correctly

#### T16: Coverage Hardening
- Run `pytest --cov`, all modules ≥90%
- All 3 benchmarks green
- Guardrail tests exercised:
  - Import check: deterministic modules don't import LLM
  - Token budget: exceeded → rejected
  - AC count: >30 → rejected
  - Truncated response → rejected

---

## Current Status

| Field | Value |
|---|---|
| Phase | Phase 2 (T6: RTM Scorer next) |
| Next Action | T6 — RTM scorer (deterministic scoring: AC coverage, type distribution, orphans) |
| Tests | 178 passing |
| Blockers | None |
